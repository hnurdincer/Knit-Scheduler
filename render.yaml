services:
  - type: web
    name: knit-scheduler
    env: python
    buildCommand: bash build.sh
    startCommand: gunicorn core.wsgi:application
    envVars:
      - key: PYTHON_VERSION
        value: 3.12
      - key: DEBUG
        value: "False"
      - key: ALLOWED_HOSTS
        value: "*"
